name = "audience-bot"
main = "src/index.js"
compatibility_date = "2026-09-01"

# Публичные preview-адреса на каждую версию не нужны - лишние двери наружу.
preview_urls = false

kv_namespaces = [
  { binding = "AUDIENCE_KV", id = "c88afc46084c4369809bd9c3ad694045" }
]

# Секреты (BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET, SUBSCRIBERS_API_SECRET, OWNER_CHAT_ID)
# сюда НЕ идут - только через `wrangler secret put`.